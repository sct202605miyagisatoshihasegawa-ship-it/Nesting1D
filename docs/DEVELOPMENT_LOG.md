# Nesting1D 開発ログ

## 2026-07-26：チェック1 要件・計算式の確定
- 実装した内容：確定仕様を判断記録へ追加し、計算ルール、代表例・境界例、期待入出力スキーマを文書化。本体コード、API、画面、保存機能は未実装。
- 実装理由：計算エンジン前に、計算結果へ影響する規則と期待値を固定するため。
- 要件定義との対応：動作モード、入力制約、切断方向、捨て切り、鋸刃厚、残材判定、最適化、再現性、JSON構造。
- 採用した設計：整数mm、純粋な入出力JSON、候補内最少母材本数、決定的比較順、計算と保存の分離。
- 変更ファイル：`DECISIONS.md`、`CALCULATION_RULES.md`、`CALCULATION_EXAMPLES.md`、`INPUT_OUTPUT_SCHEMA.md`、`DEVELOPMENT_LOG.md`、`TEST_RESULTS.md`。
- 実行したテスト：文書の存在・内容・差分、JSON構文、手計算値。本体自動テストは未実施（本体未実装）。
- テスト結果：`TEST_RESULTS.md` に記録。
- 未解決事項：人間による計算規則、代表例、期待JSONの承認。
- JavaScript移植時の注意：整数演算、正規化順、比較キー、列挙値、理由コードを共通化する。

## 2026-07-26：チェック2 計算エンジン完成
- 実装した内容：入力・結果モデル、切断長計算、残材判定、2モード、パターン集約、候補比較、再現性テスト。
- 実装理由：画面から独立した計算機能を完成させるため。
- 要件定義との対応：第4、6〜11、19、20章。
- 採用した設計：Pydantic厳密検証、純粋関数、決定的な複数貪欲候補の比較。
- 変更ファイル：`app/calculation/`、`tests/calculation/`、開発記録3ファイル。
- 実行したテスト：計算テストおよび全pytest。
- テスト結果：17件成功、0件失敗。既存警告1件は変更対象外。
- 未解決事項：代表結果の人間確認。
- JavaScript移植時の注意：整数式、候補順、比較キー、パターンキーを共通化する。

### 人間確認による修正
- 例6・7を文書期待値へ合わせ、既存残材の切出合計長、残り長、新規残材の分散を候補比較へ追加した。
- 残り0mmを `used_up` として残材・廃棄材から除外した。全18件のpytestが成功した。

## 2026-07-26：チェック3 入力画面と計算接続
- 実装した内容：2モードの入力フォーム、動的行、サーバー検証、計算エンジン接続、最小結果表示、レスポンシブ対応。
- 実装理由：Web画面から確定済み計算エンジンを安全に利用可能にするため。
- 要件定義との対応：第4〜6、12.1、18、19.5章。
- 採用した設計：POST後に同一テンプレートを再描画し、入力値・日本語エラー・結果を保持。計算は既存エンジンだけで実行。
- 変更ファイル：`app/main.py`、`app/templates/index.html`、`app/static/css/style.css`、`app/static/js/input.js`、`tests/test_web_calculation.py`、開発記録2ファイル。
- 実行したテスト：全pytest、差分範囲・空白確認。
- テスト結果：27件成功、0件失敗。既存警告1件。

## 2026-07-26：チェック4 結果画面と現場モード完成
- 実装した内容：結果ダッシュボード、集約パターン、母材カード、在庫結果、切断手順、5画面切替、レスポンシブ表示を実装。
- 実装理由：計算結果を管理者の確認と現場の機械操作の両方に使える形で提示するため。
- 要件定義との対応：第12.2〜12.5章、第13章、第18章、第19.5章。
- 採用した設計：既存`CalculationResult`だけを表示用に決定的に整形し、計算式は画面へ持ち込まない。表に依存せずカードを基本とし、同一パターンは使用回数付きで集約表示する。
- 変更したファイル：`app/main.py`、`app/templates/index.html`、`app/static/css/style.css`、`app/static/js/input.js`、`tests/test_web_calculation.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：`pytest -q tests/test_web_calculation.py`、`pytest -q`、`git diff --check`、変更範囲確認。
- テスト結果：32件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：`INPUT_OUTPUT_SCHEMA.md`の古い`scrap`表記は、`DECISIONS.md`の`used_up`が最新仕様であり、後の文書整合性確認で修正が必要。結果画面の実機・スマートフォン人間確認は未実施。
- 将来JavaScriptへ移植する際の注意点：計算結果の列挙値、パターン順、母材順、理由コードを共通化し、今回の表示用ラベル変換と計算処理を混在させない。
- 新しい仕様判断：なし。確定済みの結果モデルと表示要件のみを使用した。
- 未解決事項：`INPUT_OUTPUT_SCHEMA.md`の古い`scrap`表記は、`DECISIONS.md`の`used_up`が最新仕様であり、後の文書整合性確認で修正が必要。
- JavaScript移植時の注意：今回のJavaScriptは行操作と表示切替だけで、計算・最終検証を含まない。

### 人間確認修正：未使用在庫の理由と材料区分
- 実装した内容：未使用在庫へ材料区分を追加し、充足済み・寸法不足・追加購入抑制の理由コードを区別。保有新品母材の未使用分も結果へ追加し、画面の見出し・日本語理由を修正。
- 実装理由：必要部材充足後の材料が寸法不足と表示される混乱を解消するため。
- 採用した設計：必要数の有無を材料処理前に判定し、計算結果の理由コードを表示側で固定日本語へ変換する。
- 変更したファイル：`app/calculation/engine.py`、`tests/calculation/test_inventory.py`、`app/main.py`、`app/templates/index.html`、`tests/test_web_calculation.py`、開発記録3ファイル。
- 実行したテスト：全`pytest`、例6・7、未使用在庫代表例、`git diff --check`。
- テスト結果：36件成功、0件失敗。例6・7の割り当て、追加購入数、完成数に変更なし。
- 未解決事項：実画面での未使用在庫カードの人間再確認。
- JavaScript移植時の注意：`source_type`と3種類の理由コードを同じ列挙値で維持し、表示側で理由を推測しない。

## 2026-07-26：チェック5 保存・再読込・HTML出力
- 実装した内容：`data/`限定のJSON・HTML正式保存、東京時刻の管理番号、競合確認、安全な置換と復旧、保存済みJSON一覧・読込、端末ダウンロード、印刷用単体HTML、未保存状態と3択確認を実装した。
- 実装理由：再編集可能な正式データと、現場で閲覧・印刷・共有できる端末コピーを安全に提供するため。
- 要件定義との対応：第13〜18章、第19.1・19.2章、および確定した初期版ハイブリッド保存方式。
- 採用した設計：計算エンジンを再利用する独立保存層、`data/`の固定ルート、管理番号形式検証、一時ファイル＋`os.replace`、失敗時復旧、Jinja2自動エスケープ、正式保存成功だけを保存済みとするクライアント状態管理。
- 変更したファイル：`.gitignore`、`data/.gitkeep`、`app/storage.py`、`app/exporting.py`、`app/main.py`、`app/templates/index.html`、`app/templates/report.html`、`app/static/js/input.js`、`app/static/css/style.css`、`tests/test_checkpoint5.py`、関連文書。
- 実行したテスト：`pytest -q tests/test_checkpoint5.py`、全`pytest`、`git diff --check`、Git除外規則確認。
- テスト結果：52件成功、0件失敗。既存36件を維持。既存警告1件。
- 未解決事項：PC・スマートフォン実機での保存、読込、ダウンロード、3択確認、印刷レイアウトの人間確認。
- 将来JavaScriptへ移植する際の注意点：保存形式・列挙値を維持し、計算処理を保存UIへ重複実装しない。PWA化時も正式保存と端末コピーの状態を区別する。

### チェック5 人間確認修正：計算POST時の離脱確認
- 実装した内容：計算フォームの意図したPOST中だけ`beforeunload`を抑止し、二重送信ロックと離脱抑止を別状態に分離。送信不成立時と`pageshow`時にボタン・処理中表示を復元した。
- 実装理由：未保存入力の通常計算でブラウザ離脱確認が表示され、キャンセル後に計算ボタンが無効のまま残る不具合を解消するため。
- 変更したファイル：`app/static/js/input.js`、`tests/test_checkpoint5.py`、開発記録2ファイル。
- 実行したテスト：`pytest`、`git diff --check`。
- テスト結果：53件成功、0件失敗。既存警告1件。
- 未解決事項：実ブラウザで、計算POSTでは確認なし、通常離脱では確認あり、キャンセル後も操作可能であることの再確認。

## 2026-07-26：UI修正 第1段階（ダークテーマ基礎）
- 実装した内容：既存画面へダークテーマの共通色変数、背景、カード、フォーム、緑系フォーカス、ボタン、タブ、結果カードの基礎スタイルを適用した。HTML構造とJavaScriptは変更していない。
- 実装理由：次段階の構造変更に先立ち、既存機能を維持したまま画面全体の配色と基本的な視認性を統一するため。
- 要件定義との対応：第19.5章のPC・スマートフォン対応、および今回指定されたUI第1段階。
- 採用した設計：色・影・フォーカスを`:root`のCSS変数へ集約し、既存セレクターとレスポンシブ規則を維持して差分を限定した。
- 変更したファイル：`app/static/css/style.css`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：全`pytest`、`git diff --check`、変更範囲確認。
- テスト結果：53件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：PC・スマートフォン実機でのコントラスト、フォーカス表示、横スクロール、カードの見え方の人間確認。
- 将来JavaScriptへ移植する際の注意点：状態色はCSS変数とクラスで管理し、JavaScriptへ色コードを重複させない。

## 2026-07-26：UI修正 第2段階（識別情報・固定ルール）
- 実装した内容：上部の開発環境カードを計算結果識別情報カードへ置換し、管理番号・計算日時・計算モードを表示した。計算成功時の日時、選択モード、保存・読込時の管理番号と更新日時を既存データへ連動した。切断条件の3入力を維持し、編集不可の固定ルール3項目を説明表示として追加した。
- 実装理由：計算結果の識別に必要な情報と、利用者が変更できない切断規則を入力項目から明確に分離するため。
- 要件定義との対応：第5.2章、第7・8章、第13・16章、第19.5章、および今回指定されたUI第2段階。
- 採用した設計：計算日時は計算成功時だけサーバーの東京時刻を表示し、保存・読込では既存APIの更新日時を使用する。管理番号は従来どおり正式保存時にのみ発行する。固定ルールは`aside`と`dl`で表現し、フォーム入力にはしない。
- 変更したファイル：`app/main.py`、`app/templates/index.html`、`app/static/css/style.css`、`app/static/js/input.js`、`tests/test_main.py`、`tests/test_web_calculation.py`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：関連Web・保存テスト、全`pytest`、`git diff --check`、変更範囲確認。
- テスト結果：54件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：PC・スマートフォン実機での上部2カラム、識別情報更新、固定ルールの可読性確認。
- 将来JavaScriptへ移植する際の注意点：日時表示とモード表示のラベル変換を識別情報表示関数へ集約し、計算ロジックへ混在させない。

## 2026-07-26：計算完了後の結果位置修正
- 実装した内容：計算フォーム送信時に一時フラグを保存し、正常な結果ページでダッシュボードタブを選択後、結果タブ先頭へ滑らかにスクロールする処理を追加した。入力エラー・計算失敗・送信キャンセル時はスクロールしない。
- 実装理由：通常POST後にブラウザーがページ先頭を表示するため、計算結果へ利用者を直接案内する必要があるため。
- 要件定義との対応：第12.2章の結果把握、第19.5章のPC・スマートフォン対応、および今回指定された計算完了後の表示位置修正。
- 採用した設計：既存のサーバーPOSTと検証を維持し、`sessionStorage`の1回限りのフラグと結果タブの存在を成功条件に使用する。`pageshow`後にダッシュボードを選択し、`scrollIntoView`を実行する。
- 変更したファイル：`app/static/js/input.js`、`app/static/css/style.css`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：関連Web・保存テスト、全`pytest`、`git diff --check`、変更範囲確認。
- テスト結果：55件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：PC・スマートフォン実機での滑らかな移動位置とブラウザーごとの動作確認。
- 将来JavaScriptへ移植する際の注意点：計算方式をクライアント側へ移す場合も、成功状態を確定してから結果表示とスクロールを行う。

## 2026-07-26：UI修正 第3段階（インラインバリデーション）
- 実装した内容：切断条件、必要部材、在庫本数、既存残材の数値入力をサーバー検証結果と欄単位で対応付け、赤枠・具体的な赤文字・`aria-invalid`を表示した。正常欄は強調せず、入力開始時に該当欄だけエラー表示を解除する。エラー再描画時は最初のエラー欄へ滑らかに移動してフォーカスする。
- 実装理由：全体エラーだけでは修正対象が分かりにくく、特に複数行とモバイルで入力欄と原因を近接表示する必要があるため。
- 要件定義との対応：第5・6・18章、第19.5章、および今回指定されたUI第3段階。
- 採用した設計：既存のサーバー検証を正とし、フィールドキーへメッセージを対応付ける。切断不能判定は計算エンジンと同じ`左端捨て切り＋鋸刃厚＋部材寸法＋鋸刃厚 > 新品母材長`を使用する。複数行キーは送信時の行番号で生成し、再描画順と一致させる。
- 変更したファイル：`app/main.py`、`app/templates/index.html`、`app/static/css/style.css`、`app/static/js/input.js`、`tests/test_web_calculation.py`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：関連Web・JavaScript/CSS契約テスト、全`pytest`、`git diff --check`、変更範囲確認。
- テスト結果：59件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：PC・スマートフォン実機での赤色コントラスト、最初のエラーへの移動位置、ソフトウェアキーボード表示時のフォーカス確認。
- 将来JavaScriptへ移植する際の注意点：クライアント検証を追加する場合もサーバー・計算エンジンと同じ整数範囲と切断可能長式を共有し、行キーを表示順と一致させる。

## 2026-07-26：UI修正 第4段階（結果5タブ・入力スナップショット）
- 実装した内容：入力条件・計算条件、ダッシュボード、パターン一覧、母材一覧、切断手順の5タブを計算前から常設した。未計算時は入力タブ、計算成功時はダッシュボード、入力エラー時は入力タブを選択する。計算成功時の検証済み入力から、条件・固定ルール・必要部材集計・寸法別本数のスナップショットを入力タブ内へ追加した。
- 実装理由：計算前から画面構成を把握でき、計算後に使用した条件と結果を同じタブ領域で確認できるようにするため。
- 要件定義との対応：第5章、第12.1〜12.5章、第13章、第19.5章、および今回指定されたUI第4段階。
- 採用した設計：既存フォームを入力タブとして再利用し、結果パネルIDとタブ制御を維持した。スナップショットは計算成功時にサーバー側で生成し、再計算までDOM上の表示値を更新しない。未計算の結果タブは無効化せず空状態を表示する。
- 変更したファイル：`app/main.py`、`app/templates/index.html`、`app/static/css/style.css`、`app/static/js/input.js`、`tests/test_web_calculation.py`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：関連Web・JavaScript契約テスト、全`pytest`、`git diff --check`、変更範囲確認。
- テスト結果：62件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：PC・スマートフォン実機での5タブ横スクロール、空状態、スナップショットの可読性、キーボード操作の人間確認。
- 将来JavaScriptへ移植する際の注意点：クライアント計算へ移行しても、編集フォームと最後に成功した計算スナップショットを別状態として保持する。

### 2026-07-26：UI修正 第4段階 配置訂正（タブ共通表示窓）
- 実装した内容：訂正指示に合わせ、大きな計算ボタンの直後へ5タブを配置し、その直下の単一`tab-content`内で入力条件、ダッシュボード、パターン、母材、切断手順の5パネルを切り替える構造へ修正した。管理・アクションと共通情報を共通窓の下へ配置した。
- 実装理由：前回は入力条件スナップショットを編集フォーム内へ追加し、入力タブがフォーム全体を指していたため、添付UIの「同じ表示窓で内容だけを交換する」構造と異なっていた。
- 採用した設計：入力スナップショットを`conditions-view`へ移し、JavaScriptはフォームを隠さず`.result-panel`だけを切り替える。フォーム外へ移した共通情報は`form="calculation-form"`で既存POST・保存対象へ関連付け、個別の変更検知を維持する。
- 変更したファイル：`app/templates/index.html`、`app/static/js/input.js`、`app/static/css/style.css`、`tests/test_web_calculation.py`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：関連Web・JavaScript契約テスト、全`pytest`、`git diff --check`、変更範囲確認。
- テスト結果：63件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：PC・スマートフォン実機での共通窓の切替、指定順序、共通情報の入力・保存・読込、スクロール位置の人間確認。
- 将来JavaScriptへ移植する際の注意点：タブはパネルIDだけを切り替え、編集フォーム自体の表示状態とは分離する。

## 2026-07-26：在庫モード入力セクション順序修正
- 実装した内容：在庫母材・残材活用モードでは、切断条件の後を「在庫情報→必要部材→計算する」の順に変更した。必要母材算出モードでは従来順を維持する。
- 実装理由：在庫モードで在庫条件を先に入力できるよう、利用者指定の表示順へ合わせるため。
- 要件定義との対応：第4.2節および第5.4節の在庫モード専用入力。
- 採用した設計：既存セクションをモード切替時に移動し、入力要素、初期値、行操作、検証、計算処理は変更しない。
- 変更したファイル：app/static/js/input.js、tests/test_checkpoint5.py、docs/DEVELOPMENT_LOG.md、docs/TEST_RESULTS.md。
- 実行したテスト：全pytest、git diff --check、変更範囲確認。
- テスト結果：docs/TEST_RESULTS.mdに記録。
- 未解決事項：PC・スマートフォンでのモード切替時の表示順を目視確認する。
- 将来JavaScriptへ移植する際の注意点：入力値を再生成せず既存DOMを移動しているため、行追加・削除とフォーム送信の状態を維持できる。
- 新しい仕様判断：なし。利用者が明示した表示順のみを反映した。

## 2026-07-26：UI修正 第5段階（管理・アクション）
- 実装した内容：管理・アクションの表面を保存とリセットの2操作へ整理し、未保存状態を見出し付近へ表示した。保存済みJSON読込、JSONダウンロード、HTMLダウンロードは入力条件・計算条件タブ末尾の「その他の操作」折りたたみへ移動した。新規作成と入力リセットはリセットへ統合した。
- 実装理由：主要操作を明確にしつつ、既存の補助機能と未保存保護を維持するため。
- 要件定義との対応：保存・再読込・HTML出力、未保存確認、および今回指定されたUI第5段階。
- 採用した設計：既存の単一dirty変数、setDirty、protectedAction、未保存確認dialogを再利用した。リセットは従来の新規作成と同じ初期URLへの遷移とし、共通情報を含む全入力・結果・識別情報を初期状態へ戻す。
- 変更したファイル：app/templates/index.html、app/static/js/input.js、app/static/css/style.css、tests/test_web_calculation.py、tests/test_checkpoint5.py、docs/DEVELOPMENT_LOG.md、docs/TEST_RESULTS.md。
- 実行したテスト：関連Web・JavaScript契約テスト、全pytest、git diff --check、変更範囲確認。
- テスト結果：関連45件成功。全件結果はTEST_RESULTS.mdへ記録。
- 未解決事項：PC・スマートフォンでの折りたたみ操作、未保存表示、保存・読込・リセット確認ダイアログの目視確認。
- 将来JavaScriptへ移植する際の注意点：dirty状態を増やさず、入力変更、保存、読込、リセットの状態遷移を単一管理する。
- 新しい仕様判断：なし。既存の新規作成・入力リセット仕様を統合して利用者指示へ合わせた。

## 2026-08-01：候補選別に対応した未使用理由
- 実装した内容：残材不使用候補の理由コードを候補選別全体を表す値へ変更し、Web・HTMLの日本語表示、JSON保存、関連テストと文書を整合させた。
- 実装理由：追加購入数が同じでも、廃棄材・残材・作業性などの評価により残材不使用候補が選ばれるため。
- 要件定義との対応：在庫モードの未使用在庫表示、結果JSON、HTML出力、再現性。
- 採用した設計：計算結果とJSONは理由コードを保持し、日本語文言への変換は既存の表示層へ集約する。候補比較キーから理由コードを除外する既存設計を維持する。
- 変更したファイル：`app/calculation/engine.py`、`app/main.py`、理由コード関連テスト、`docs/DECISIONS.md`、`docs/INPUT_OUTPUT_SCHEMA.md`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：理由コード専用、候補選別、候補変換、候補接続、在庫、Web、保存・HTML、計算層全体、全pytest、旧コード検索、`git diff --check`。
- テスト結果：全131件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：選別理由の詳細内訳表示は対象外。現行コードは候補選別全体を表す。
- 将来JavaScriptへ移植する際の注意点：理由コードを共通列挙値として維持し、日本語文言は表示層で対応付ける。

## 2026-08-01：候補選別のengine接続と完全同値候補の正規化
- 実装した内容：候補専用データ型、独立した8段階選別、実データ安定キー、plan変換、required-stock 2候補・inventory 4候補のengine接続を実装した。完全同値候補では未使用理由も物理状態から共通判定し、候補順を反転してもCalculationResult全体が一致するようにした。
- 実装理由：旧比較を置換し、追加購入数、材料効率、作業性を段階的に比較しつつ、生成順へ依存しない再現性を確保するため。
- 要件定義との対応：第4章、第8〜11章、第19.4章。
- 採用した設計：整数mm、廃棄材・残材の独立許容値、既存rules・patternsの再利用、reason_codeを含めない不変安定キー、`select_candidate()`への最終選択集約。
- 変更したファイル：`app/calculation/selection.py`、`app/calculation/engine.py`、`app/main.py`、関連計算・Web・保存テスト、候補選別関連文書。
- 実行したテスト：完全同値境界、未使用理由、候補選別、候補変換、候補接続、在庫、Web、保存・HTML、計算層全体、全pytest、旧仕様検索、`git diff --check`。
- テスト結果：全132件成功、0件失敗。既存のStarlette非推奨警告1件。
- 未解決事項：廃棄材・残材許容値の本番調整値は未決定で、現在はいずれも0mm。
- 将来JavaScriptへ移植する際の注意点：8段階順位、整数許容値、安定キーの項目順、未使用理由の優先順位を同一にする。


## 2026-08-03：残材・廃棄材の50/51mm固定境界
- 実装した内容：`remainder_class()`を鋸刃厚非依存の1引数関数へ変更し、残り0mmを使い切り、1～50mmを廃棄材、51mm以上を残材とした。engineの全呼出箇所、候補評価・CalculationResultの境界テスト、候補選択回帰テスト、関連仕様文書を更新した。
- 実装理由：ケース7の人間確認で鋸刃厚3mm時に51～53mmが廃棄材となる不整合が見つかり、確定仕様の固定境界と実装が一致していなかったため。
- 要件定義との対応：第5.2節の廃棄判定基準寸法、第8章の残材・廃棄材定義、第9.2節の候補選別、第11章の再現性。
- 採用した設計：残材分類から`kerf`引数を除き、切出可否・使用長・残り長計算は変更しない。候補変換と最終結果は同じ`remainder_class()`を使用する。
- 変更したファイル：`app/calculation/rules.py`、`app/calculation/engine.py`、`tests/calculation/test_required_stock.py`、`tests/calculation/test_candidate_conversion.py`、`docs/REQUIREMENTS.md`、`docs/CALCULATION_RULES.md`、`docs/CALCULATION_EXAMPLES.md`、`docs/DEVELOPMENT_ROADMAP.md`、`docs/DECISIONS.md`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：境界・候補変換・候補選択・候補接続・在庫・Webの関連pytest、全pytest、`git diff --check`、`git status --short`。
- テスト結果：関連108件成功、全142件成功、失敗0件。既存のStarlette非推奨警告1件。
- 既存結果の変更：旧境界を固定していた55mm廃棄・56mm残材の期待値を、50mm廃棄・51mm残材へ変更した。新規回帰例では旧境界時に選ばれる50mm廃棄候補から、修正後は廃棄材総量が少ない51mm残材候補へ選択結果が変わる。
- 未解決事項：修正後のケース7について、実画面で残り50mmが廃棄材、51mm以上が残材と表示されることの人間再確認。`NOT_NEEDED`の日本語表示改善は別の受入確認改善事項として保留する。
- 将来JavaScriptへ移植する際の注意点：0/1/50/51mmの固定境界と鋸刃厚非依存性を同じ列挙値・境界テストで維持する。


## 2026-08-04：利用者向け材料名称・使用材料一覧・切断手順の表示整合
- 実装日：2026-08-04。
- 実装した内容：利用者向け材料名を「購入新品材」「在庫新品材」「在庫残材」「発生残材」「廃棄材」「切出し部材」へ統一し、「母材一覧」を「使用材料一覧」、「使用母材数」を「使用材料数」へ変更した。切断手順はパターン内の使用材料を材料種別・元長ごとに集約し、用意する材料名・寸法・本数と、捨て切りおよび部材切断で消費する鋸刃厚の実回数をWeb・HTMLへ表示した。NOT_NEEDEDは「必要部材がすべて確保され、この在庫材料を使用する必要がないため」へ変更した。必要部材・在庫残材の行追加ボタンを各入力行群の末尾へ移動し、本文・入力・タブ・ボタン・結果・手順の文字サイズ、改行、数値単位の分離防止、モバイル配置を調整した。HTMLの廃棄判定表示を鋸刃厚非依存の固定50mmへ修正した。
- 実装理由：Web画面とHTML現場手順で材料の意味を明確にし、確定済みの固定50mm境界と利用者向け表示を一致させ、PC・スマートフォン双方の可読性と入力行追加時の操作性を改善するため。
- 要件定義との対応：第5章の入力、第8章の固定50mm境界、第12章のダッシュボード・材料一覧・現場モード、第13章の出力、第15章のHTML出力、第19.5章のPC・スマートフォン対応。
- 採用した設計：JSON内部キー、source_type、reason_code、計算結果を変更せず、既存結果から表示用の材料ラベルと材料種別・元長別の数量を決定的に集約する。未使用在庫は従来どおり別表示とし、使用材料一覧にはstock_usageだけを表示する。数値と単位は表示用spanとCSSのnowrapで一体表示する。
- 変更したファイル：`app/main.py`、`app/templates/index.html`、`app/templates/report.html`、`app/static/css/style.css`、`tests/test_web_calculation.py`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。`app/static/js/input.js`は既存の末尾追加処理で要件を満たすため変更なし。
- 実行したテスト：`pytest -q tests/test_web_calculation.py tests/test_checkpoint5.py -p no:cacheprovider`、`pytest -q -p no:cacheprovider`、旧表示検索、代表Web応答確認、在庫ケースの使用材料数確認、`git diff --check`。
- テスト結果：関連47件成功、全144件成功、失敗0件。既存のStarlette TestClient非推奨警告1件。
- 未解決事項：PC・スマートフォン実機でのフォント、タブ、材料名1行表示、横スクロール、行追加後の最終行下配置、現場手順の読みやすさ、およびA4印刷の人間確認。
- 将来JavaScriptへ移植する際の注意点：内部のsource_type・reason_code・JSONキーを維持し、今回と同じ表示ラベル対応と、材料種別・元長別の決定的な表示用集約、切断本数と鋸刃厚消費回数の対応を表示層へ移植する。
- 新しい仕様判断：なし。利用者が確定した表示仕様のみを反映し、`docs/DECISIONS.md`は更新していない。


### 2026-08-04：保存成功後のJSONダウンロード回帰修正
- 実装した内容：保存API成功後、返却された管理番号を画面状態へ反映し、未保存状態を解除した後に、同じ管理番号のJSONダウンロードを自動開始するよう`input.js`を修正した。既存のJSONダウンロードボタンも同じ関数を使用する。
- 実装理由：保存ボタンはサーバー側のJSON・HTML正式保存までは成功していたが、端末向けJSONコピー取得を開始しておらず、利用者が従来期待する保存操作と一致していなかったため。
- 原因調査：`/api/save`の409は、既存`management_number`を`overwrite: false`で保存した場合の上書き確認用応答。フロントの保存payload、計算後のdirty状態、管理番号設定、JSON内部キーは正常だった。保存成功後にダウンロード処理がないことが直接原因で、今回の材料表示用`main.py`・`index.html`・CSS変更との直接関係はなかった。
- 採用した設計：409と確認後の`overwrite: true`再送を維持し、API成功・管理番号確定後だけJSONをダウンロードする。正式保存時のJSON・HTML同時生成と、HTMLの手動ダウンロード導線は変更しない。
- 変更したファイル：`app/static/js/input.js`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。既存の未コミット表示変更は保持した。
- 実行したテスト：保存・上書き関連10件、JSONダウンロード関連2件、HTML出力関連3件、チェック5全25件、全pytest、`git diff --check`。
- テスト結果：全146件成功、失敗0件。既存のStarlette TestClient非推奨警告1件。
- 未解決事項：Chrome実機で、初回保存後にJSONがダウンロードされること、既存管理番号の保存では確認ダイアログ後に上書きとJSONダウンロードが完了すること、HTMLダウンロードボタンが従来どおり動作することの人間確認。
- 将来JavaScriptへ移植する際の注意点：正式保存成功、管理番号確定、dirty解除、コピー取得の順序を維持し、409上書き確認を失敗扱いとして途中終了させない。


### 2026-08-04：ダウンロード操作の管理・アクション内配置
- 実装した内容：「その他の操作」にあったJSONダウンロード・HTMLダウンロードボタンを「管理・アクション」へ移し、保存→JSONダウンロード→HTMLダウンロード→リセットの順で配置した。「その他の操作」は保存済みJSON読込が残るため維持した。PCでは2列、600px以下では1列とし、ボタン名を折り返さないCSSを追加した。
- 実装理由：管理番号、未保存状態、保存、ダウンロード、リセットを同じ管理領域へ集約し、操作の発見性と順序を明確にするため。
- 採用した設計：既存IDとJavaScriptイベント処理を維持し、`index.html`のDOM配置と`style.css`の表示だけを変更する。
- 変更したファイル：`app/templates/index.html`、`app/static/css/style.css`、`tests/test_web_calculation.py`、`tests/test_checkpoint5.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：関連Web・チェック5テスト、全pytest、`git diff --check`。
- テスト結果：関連50件成功、全147件成功、失敗0件。既存のStarlette TestClient非推奨警告1件。
- 未解決事項：PC・スマートフォン実機で4ボタンの順序、1段表示、文字切れ・横スクロール・不自然な折返しがないことの人間確認。
- 将来JavaScriptへ移植する際の注意点：ボタンIDとイベント処理の対応を維持し、DOM配置変更だけで機能を再利用できる構造を保つ。


### 2026-08-05：使用材料カードの在庫区分重複表示削除
- 実装日：2026-08-05。
- 実装した内容：「使用材料一覧」タブの各材料カード本文から「在庫区分」行を削除し、本文を「元の長さ」「パターン」「切出し部材」「使用後」の4項目に限定した。カード右上の材料区分は維持した。
- 実装理由：材料区分はカード右上に表示済みであり、本文内の同一情報が重複していたため。
- 要件定義との対応：第12.4節の母材一覧、第19.5節のPC・スマートフォン対応。
- 採用した設計：Web画面の材料カード用テンプレートから重複行だけを削除し、既存のカード構造・CSS・表示用データは変更しない。在庫残材・在庫新品材・購入新品材の各カードで右上表示と本文4項目を回帰テストする。
- 変更したファイル：`app/templates/index.html`、`tests/test_web_calculation.py`、`docs/DEVELOPMENT_LOG.md`、`docs/TEST_RESULTS.md`。
- 実行したテスト：`pytest -q tests/test_web_calculation.py -p no:cacheprovider`、`pytest -q -p no:cacheprovider`、`git diff --check`、変更差分・変更範囲確認。
- テスト結果：関連25件成功、全148件成功、失敗0件。既存のStarlette TestClient非推奨警告1件。
- 未解決事項：PC・スマートフォン実機で、3種類の材料カードに文字切れ・横スクロール・不自然な余白がないことの人間確認。
- 将来JavaScriptへ移植する際の注意点：材料区分の内部データは維持し、カードでは右上だけに表示して本文へ重複表示しない。
- 新しい仕様判断：なし。利用者が指定した表示修正のみのため、`docs/DECISIONS.md`は更新していない。


## 2026-08-05：公開版V1の旧サーバー保存経路完全撤去
- 実装日：2026-08-05。
- 実装した内容：旧保存ボタン、保存済み一覧、旧サーバー読込、上書き確認、`POST /api/save`、`GET /api/records/{management_number}`、旧再ダウンロードAPI、`RecordStorage`を撤去した。JSON・HTMLはメモリ内直接出力、JSON読込はFile APIだけに統一し、静的JavaScript URLへ版識別子を付与した。
- 実装理由：公開版V1の「サーバーは計算と出力生成だけを行い、利用者データを保持しない」という確定ポリシーへ一致させるため。
- 要件定義との対応：第14章JSON保存、第15章HTML出力、第16章管理番号、第17章変更確認、チェック5。
- 採用した設計：形式・管理番号・レコード検証は`app/records.py`へ維持し、ファイルI/O責務だけを削除する。既存`data/`実データは変更せず、アプリからの参照と書込みをなくす。
- 変更したファイル：`app/main.py`、`app/templates/index.html`、`app/static/js/input.js`、`app/static/css/style.css`、`app/records.py`、`tests/test_checkpoint5.py`、`tests/test_web_calculation.py`、要件・ロードマップ・判断・開発記録。`app/storage.py`は削除した。
- 実行したテスト：関連pytest、全pytest、Python構文検査、旧API・旧UI・保存層参照検索、`git diff --check`。
- テスト結果：関連68件成功、全165件成功、失敗0件。既存のStarlette非推奨警告1件。
- 人間確認結果：JSON・HTML独立出力、出力状態、端末JSON復元と再計算前状態、旧API通信不存在、`data/`非増加、PC・スマートフォン配置の全項目に合格。
- 未解決事項：保存方式移行に関する未解決事項なし。
- 将来JavaScriptへ移植する際の注意点：File API読込と出力状態はページ内だけに保持し、サーバー保存やブラウザ永続ストレージを暗黙に追加しない。

## 2026-08-05：公開版V1機能凍結
- 実施日：2026-08-05。
- 記録した内容：公開版V1の機能境界、凍結後に許可する変更、V2以降へ回す変更、非永続データ原則、少人数での限定公開方針を正式に記録した。
- 基準コミット：`ac82ed5 公開版V1の保存方式を端末直接出力へ移行`。
- 凍結理由：現在実装済みの計算、画面、端末向けJSON・HTML出力、File API読込について、自動テストと人間による受入確認が完了したため。限定試用前の機能追加を止め、品質・安全性・運用確認へ集中する。
- 要件定義との対応：現行の計算範囲、画面構成、公開版V1の保存方式、管理番号、リリース前チェックと整合する。要件変更は行っていない。
- 採用した設計：詳細な非永続データ判断は`DECISIONS.md`の判断17を参照し、判断18にV1機能境界、変更分類、限定公開運用を集約する。ロードマップでは機能凍結を完了状態とし、次工程をチェック7のリリース前確認とする。
- 変更したファイル：`docs/DECISIONS.md`、`docs/DEVELOPMENT_LOG.md`、`docs/DEVELOPMENT_ROADMAP.md`。
- 実行した確認：文書間のV1対象・対象外・非永続保存・限定公開・次工程の整合確認、変更範囲確認、`git diff --check`。
- テスト結果：文書のみの変更であり、コードおよびテストは変更・実行していない。
- 未解決事項：限定試用の具体的な日時、URL共有先、HTTPS・アクセス制限等の公開環境設定はリリース前確認で確定する。
- 次工程：チェック7「リリース前最終確認」。READMEおよび一般利用者向け説明書の最終仕上げは、限定試用で実際の利用方法と注意点を確認した後に行う。
- 将来JavaScriptへ移植する際の注意点：PWA化やブラウザ永続保存はV1へ追加せず、V2以降の独立した要件として扱う。

## 2026-08-06：Cloud Run公開対応 第2-A 公開環境の基本設定
- 実装日：2026-08-06。
- 実装した内容：`APP_ENV`による本番識別、本番時のFastAPIドキュメント無効化、共通セキュリティヘッダー、HTMLキャッシュ抑止、noindexメタタグ、`robots.txt`を追加した。
- 実装理由：公開版V1の機能を変更せず、Cloud Runでの限定公開に必要な基本的な露出・埋込み・キャッシュ対策を加えるため。
- 要件定義との対応：安全性、公開版V1非永続方針、リリース前チェック、判断18の公開環境問題・セキュリティ修正。
- 採用した設計：環境判定は標準ライブラリだけの小関数とし、ヘッダーは単一HTTPミドルウェアで既存値を上書きせず付与する。HTML判定はContent-Typeで行い、静的ファイルへno-storeを誤適用しない。
- 変更したファイル：`Dockerfile.prod`、`app/main.py`、`app/templates/index.html`、`app/templates/report.html`、`tests/test_production_security.py`、開発記録3ファイル。
- 実行したテスト：`pytest -q tests/test_production_security.py tests/test_main.py tests/test_web_calculation.py tests/test_checkpoint5.py -p no:cacheprovider`、`pytest -q -p no:cacheprovider`、`git diff --check`、差分・変更範囲確認。
- テスト結果：関連84件成功、全179件成功、失敗0件。既存のStarlette TestClient非推奨警告1件。
- 未解決事項：CSP、HTTPSリダイレクト、Trusted Host、アクセス制限、レート制限等は第2-A対象外。Cloud Run実環境でのヘッダー確認とドキュメント404確認はデプロイ後に必要。
- 人間確認が必要な項目：Cloud Run上で公開URL、`robots.txt`、HTMLキャッシュ抑止、ダウンロード動作を確認する。
- 将来JavaScriptへ移植する際の注意点：PWAでキャッシュ戦略を導入する際は、今回のHTML no-storeとService Workerの責務を明示的に再設計する。
- 次工程：公開対応の後続工程。第2-Aを超える制御は今回実装しない。

## 2026-08-06：Cloud Run公開対応 第2-B HTTP本文サイズ上限と413応答
- 実装日：2026-08-06。
- 実装した内容：`MAX_REQUEST_BODY_BYTES`解析、既定256 KiB、Content-Length事前判定、ASGI分割受信量判定、413・不正Content-Lengthの400を追加した。
- 実装理由：Cloud Runのプラットフォーム制限とは別に、アプリのルート処理前で過大本文を拒否し、メモリ・処理負荷を抑えるため。
- 要件定義との対応：安全性、性能、公開環境問題への対応、リリース前確認。
- 採用した設計：`app/request_limits.py`の純粋ASGIミドルウェアで`receive`を一度だけラップし、チャンクを複製せずバイト数だけ累積する。第2-Aヘッダーミドルウェアを外側に保つ登録順とした。
- 変更したファイル：`app/main.py`、`app/request_limits.py`、`Dockerfile.prod`、`tests/test_request_body_limit.py`、開発記録3ファイル。
- 実行したテスト：`pytest -q tests/test_request_body_limit.py tests/test_production_security.py -p no:cacheprovider`、`pytest -q -p no:cacheprovider`、`git diff --check`、差分・変更禁止範囲確認。
- テスト結果：関連39件成功、全204件成功、失敗0件。既存のStarlette TestClient非推奨警告1件。
- 未解決事項：256 KiBは初回負荷試験前の仮上限。正常な最大入力の実測と負荷試験後に再評価が必要。
- 人間確認が必要な項目：Cloud Run上で通常フォーム・JSON/HTML出力、413・400、共通ヘッダーを確認する。
- 将来JavaScriptへ移植する際の注意点：クライアント側の入力制御だけに依存せず、サーバー受信上限を独立した境界として維持する。
- 次工程：負荷試験および後続の公開対応。レート制限・タイムアウト等は今回実装しない。
