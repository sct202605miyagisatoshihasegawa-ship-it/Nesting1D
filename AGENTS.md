# Nesting1D Codex開発指示

## 1. 作業範囲

- 作業対象は `/workspace/nesting1d` のみとする。
- プロジェクト外のファイルを変更しない。
- プロジェクト外へファイルを作成しない。
- Dockerソケットへアクセスしない。
- Windows側の他のフォルダへアクセスしない。
- 管理者権限を使用しない。

## 2. 最優先資料

作業開始前に、必ず以下を読むこと。

- docs/REQUIREMENTS.md
- docs/DEVELOPMENT_ROADMAP.md
- docs/DECISIONS.md
- docs/DEVELOPMENT_LOG.md
- docs/TEST_RESULTS.md

要件定義書と `docs/DECISIONS.md` を実装判断の基準とする。

資料間に矛盾、不明点、または計算結果に影響する曖昧さがある場合は、
推測で仕様を決めず、作業を停止して利用者へ確認すること。

## 3. 開発単位

- 開発は `docs/DEVELOPMENT_ROADMAP.md` のフェーズ単位で行う。
- 指定されたチェックポイントまでは、細かい人間確認を求めず進める。
- フェーズ開始時に、実施内容と主な変更対象を一度だけ整理する。
- フェーズ途中の確認は、自動テストと開発ログで代替する。
- 指定されたフェーズを超えて先の機能を実装しない。
- 無関係な機能追加やリファクタリングを行わない。

## 4. 変更方針

- 既存コードを尊重し、必要な範囲だけ変更する。
- 既存ファイルの全面書き換えを避ける。
- ファイルを無断で削除しない。
- ファイル名を無断で変更しない。
- 設定ファイルを無断で変更しない。
- Docker関連ファイルを無断で変更しない。
- `pyproject.toml` を無断で変更しない。
- 依存ライブラリを追加する前に、必要性と影響を説明する。
- 削除操作が必要な場合は、対象と理由を先に示す。

## 5. セキュリティ

- APIキー、アクセストークン、パスワードを保存しない。
- `.env` や認証ファイルをGitへ登録しない。
- 秘密情報をソースコード、ログ、テストデータへ含めない。
- 不要な外部通信を行わない。
- 外部通信が必要な場合は、目的を先に説明する。
- 個人パスをコード、ログ、HTML、JSONへ残さない。
- 利用者が指定した出力先以外へファイルを作成しない。

## 6. 設計方針

- 初心者が読める明快なコードを優先する。
- 関数とクラスの責務を小さく保つ。
- 計算ロジックをFastAPIから分離する。
- 計算ロジックを画面表示から分離する。
- 計算ロジックをHTTP処理から分離する。
- 計算ロジックをファイル保存から分離する。
- 入力と出力のデータ構造を明確にする。
- 同じ入力に対して同じ結果を返す。
- 将来のJavaScript移植を妨げる依存を、計算ロジックへ持ち込まない。

## 7. テスト方針

- 変更後は関連する自動テストを実行する。
- 新しい仕様には対応するテストを追加する。
- 正常値だけでなく、境界値と異常値をテストする。
- 同一入力に対する再現性をテストする。
- 既存テストを壊さない。
- テストを削除または弱体化して成功扱いにしない。
- テスト失敗を隠さない。
- 実装と無関係な警告を勝手に修正しない。
- フェーズ終了時には、可能な範囲で全テストを実行する。

## 8. 開発記録

各フェーズ終了時に、以下を更新すること。

### docs/DEVELOPMENT_LOG.md

以下を記録する。

- 実装日
- 実装した内容
- 実装理由
- 要件定義との対応
- 採用した設計
- 変更したファイル
- 実行したテスト
- テスト結果
- 未解決事項
- 将来JavaScriptへ移植する際の注意点

### docs/DECISIONS.md

新しい仕様判断または設計判断が発生した場合のみ記録する。

- 判断内容
- 採用理由
- 却下した案
- 影響範囲
- 将来見直す条件

利用者の承認なしに、要件変更を確定事項として記録しないこと。

### docs/TEST_RESULTS.md

以下を記録する。

- 対象フェーズ
- 実行したテスト
- 成功件数
- 失敗件数
- 修正内容
- 人間確認が必要な項目
- 人間確認の結果

## 9. 人間確認

人間への確認は、原則としてロードマップに定めた次のタイミングで行う。

1. 要件・計算式の確定
2. 計算エンジン完成
3. 入力画面と計算接続
4. 結果画面と現場モード完成
5. 保存・再読込・HTML出力完成
6. 実務想定の通し操作
7. リリース前最終確認

計算結果に影響する曖昧点、セキュリティ上の問題、
作業範囲を超える変更が必要になった場合は、
チェックポイント前でも作業を停止して確認すること。

## 10. 作業終了時の報告

各フェーズ終了時に、次の形式で報告する。

1. 実装した内容
2. 実装理由
3. 変更したファイル
4. 実行したテスト
5. テスト結果
6. 更新した開発記録
7. 未解決事項
8. 人間確認が必要な項目
9. 次に進むロードマップ上のフェーズ

指定チェックポイントに到達するまでは、
小さな変更ごとに人間確認を繰り返し求めないこと。